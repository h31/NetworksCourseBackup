#pragma once
#include <iostream>

using namespace std;
class StringsHelper
{
public:
	static char* getCleanBuffer(int Size);
	static char* replaceEmptySymbols(char * sourse, int size);
};

